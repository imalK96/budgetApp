<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.imal">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".CreateBudgetMenu"></activity>
        <activity android:name=".updateCredentials" />
        <activity android:name="com.example.thanuja.fragments.ThanujaMain" />
        <activity android:name=".registerUser" />
        <activity android:name=".View" />
        <activity android:name=".addCategory" />
        <activity android:name=".userProfile" />
        <activity android:name=".MainActivity" />
        <activity android:name=".EnterIncome" />
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>